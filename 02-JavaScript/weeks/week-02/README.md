# 2주차: ES6+ 표준 문법 및 함수형 프로그래밍 입문

## 📖 핵심 요약
구조 분해 할당(Destructuring) 및 전개 구문(Spread Operator) 등 모던 ES6+ 자바스크립트의 필수 편의 문법을 완전히 익히고, 코드의 가독성과 부작용(Side Effect)을 줄이기 위한 **함수형 프로그래밍(Functional Programming)** 방법론을 도입합니다.

*   **일급 객체 (First-Class Object)**: 자바스크립트의 함수는 변수에 할당할 수 있고, 함수의 인자로 전달될 수 있으며, 다른 함수의 결과로 반환될 수 있는 일급 객체입니다.
*   **고차 함수 (Higher-Order Function)**: 함수를 인자로 받거나 함수를 반환하는 함수로, 대표적으로 `map`, `filter`, `reduce` 등이 있습니다.
*   **커링 (Currying)**: 다중 인자를 받는 함수를 단일 인자를 받는 일련의 함수들의 연쇄로 변환하는 기법입니다. 부분 적용(Partial Application)을 가능하게 합니다.
*   **함수 합성 (Composition)**: 하나의 함수의 출력이 다른 함수의 입력이 되도록 함수들을 연결하여 크고 복잡한 연산을 잘게 쪼갠 함수들의 결합으로 해결하는 기법입니다.

## 🎯 학습 목표
1.  구조 분해 할당, 전개 연산자, 널 병합 연산자(??) 등의 모던 ES6+ 스펙을 활용하여 간결한 코드를 작성한다.
2.  `map`, `filter`, `reduce`를 순수 함수 형태로 작성하여 원본 데이터를 변경하지 않고 가공하는 법을 익힌다.
3.  커링과 합성 함수 파이프라인을 직접 작성하여 유연하고 조립하기 쉬운 데이터 변환 유틸리티를 설계한다.

## 🧪 2주차 미션: 선언형 데이터 가공 파이프라인 모듈 구현
*   **미션 파일**: `mission.js`
*   **요구 사항**:
    1.  `pipe` 함수는 여러 개의 단일 인자 함수들을 입력받아, 입력받은 순서대로 실행하고 최종 결과를 반환하는 합성 함수를 만들어내야 합니다.
    2.  `curry` 함수는 임의의 개수를 지닌 인자들을 입력받는 다중 인자 함수를 커링 가능한 함수로 변환해 주어야 합니다.
    3.  커링 함수와 고차 함수를 조합하여 아래의 요구 사항을 처리하는 선언형 데이터 정제 함수 `processUsers(users)`를 작성하십시오.
        -   나이가 20세 이상인 유저만 필터링합니다. (`filter`)
        -   필터링된 유저의 정보 중 이름과 이메일 주소만 포함하는 객체로 맵핑합니다. (`map`)
        -   이름 순서대로 알파벳 오름차순 정렬합니다. (`sort`)
        -   이 모든 변환 과정은 순수 함수의 조합 및 `pipe` 함수를 통해 한 줄로 구현되는 파이프라인 형태여야 합니다.

## 📚 추천 참고서
*   *Functional JavaScript with Ramda 2026* - Chapter 1 & 2
*   *Eloquent JavaScript (4th Edition)* - Chapter 5 (Higher-Order Functions)

## 💡 학습 및 구현 참고 가이드
이번 주차 미션을 수행하고 자바스크립트 숙련도를 높이기 위한 상세 가이드는 아래 문서를 참고하십시오.
*   [2주차 개념 설명서 (explanation.md)](file:///Users/jaehkim/projects/web-basic/02-JavaScript/weeks/week-02/explanation.md): 함수 합성(`pipe`), 커링(`curry`), 불변성(Immutability)에 대한 동작 원리와 함께 이해도를 극대화할 수 있는 **심화 자율 과제**를 제공합니다.
*   **구현 핵심 규칙**:
    1.  **원본 훼손 금지(Immutability)**: `sort` 구현 시 반드시 전개 연산자(`[...]`) 등을 사용해 복사본을 만든 뒤 정렬해야 합니다.
    2.  **선언적 합성**: `processUsers`는 조건문이나 반복문 없이 오직 `pipe`, `filter`, `map`, `sort`만을 활용해 파이프라인 형태로 구현되어야 합니다.
